"""You'll implement this."""

from word_list import WordList


class Solver:
    def __init__(self, word_list: WordList):
        self.word_list = word_list

    def find_container_words(self):
        """Return every word that contains a different word from the list as a substring.

        The result is a list sorted alphabetically with no duplicates.
        Example: for ["cat", "concatenate", "dog"] the answer is ["concatenate"].
        """
        pass
